<ui:FluentWindow x:Class="MCModPlus.Views.ConfirmationDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Width="440"
                 SizeToContent="Height"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 Title="MCMod++"
                 Icon="/MCModPlus;component/Assets/AppIcon.png"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="Microsoft YaHei UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ui:TitleBar Title="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type ui:FluentWindow}}}" ShowMinimize="False" ShowMaximize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="/MCModPlus;component/Assets/AppIcon.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <Grid Grid.Row="1" Margin="22,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Width="42" Height="42" CornerRadius="21" Background="#24E81123" VerticalAlignment="Top">
                <ui:SymbolIcon Symbol="Warning24" Foreground="{StaticResource DangerBrush}" FontSize="22"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14" LineHeight="22"
                           Foreground="{StaticResource TextPrimary}" />
                <TextBlock x:Name="HintText" TextWrapping="Wrap" FontSize="12" LineHeight="18"
                           Foreground="{StaticResource TextTertiary}" Margin="0,8,0,0" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
                    <ui:Button Content="取消" Margin="0,0,10,0" IsCancel="True" Click="OnCancel" />
                    <ui:Button Content="删除" Appearance="Danger" IsDefault="True" Click="OnConfirm" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
