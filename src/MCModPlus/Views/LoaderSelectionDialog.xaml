<ui:FluentWindow x:Class="MCModPlus.Views.LoaderSelectionDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Width="420"
                 SizeToContent="Height"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 Title="MCMod++"
                 Icon="/MCModPlus;component/Assets/AppIcon.png"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="Microsoft YaHei UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ui:TitleBar Title="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type ui:FluentWindow}}}" ShowMinimize="False" ShowMaximize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="/MCModPlus;component/Assets/AppIcon.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock x:Name="DescriptionText" FontSize="14" Foreground="{StaticResource TextPrimary}" />
            <TextBlock Text="选择后会覆盖这些 Mod 当前的加载器信息。" FontSize="12" Foreground="{StaticResource TextTertiary}" Margin="0,8,0,12" />
            <ComboBox x:Name="LoaderComboBox" Height="36" SelectedIndex="0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource LoaderToText}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <ui:Button Content="取消" Margin="0,0,10,0" Click="OnCancel" />
                <ui:Button Content="应用" Style="{StaticResource GreenButton}" Click="OnConfirm" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
